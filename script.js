import Toast from "./Toast.js";


document.querySelector('button').addEventListener('click', () => {
    const toast = new Toast()

    // setTimeout(() => toast.update({ autoClose: 1000 }), 1000)
})

